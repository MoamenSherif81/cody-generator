from PIL import Image
import requests


def process_screenshot(image: Image.Image) -> str:
    # Placeholder: Call AI API to convert screenshot to DSL
    # Example: Send image to an AI service and get DSL
    return """
    row{box, box}
    """
